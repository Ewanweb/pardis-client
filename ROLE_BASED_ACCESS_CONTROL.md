# 🔐 سیستم کنترل دسترسی مبتنی بر نقش (RBAC)

## 📋 نقش‌های سیستم

### 🏢 نقش‌های مدیریتی ارشد

- **Manager** (مدیر سیستم) - دسترسی کامل به تمام بخش‌ها
- **GeneralManager** (مدیر کل) - دسترسی کامل به تمام عملیات
- **Admin** (ادمین) - دسترسی فنی و مدیریتی کامل

### 💼 نقش‌های مدیریتی تخصصی

- **EducationManager** (مدیر آموزش) - مدیریت دوره‌ها و محتوای آموزشی
- **FinancialManager** (مدیر مالی) - مدیریت امور مالی و حسابداری
- **ITManager** (مدیر IT) - مدیریت فنی و کاربران سیستم
- **MarketingManager** (مدیر مارکتینگ) - مدیریت بازاریابی و تبلیغات
- **DepartmentManager** (مدیر دپارتمان) - مدیریت بخش‌های مختلف
- **InternalManager** (مدیر داخلی) - مدیریت امور داخلی

### 👨‍🏫 نقش‌های آموزشی

- **Instructor** (مدرس) - تدریس و مدیریت دوره‌های خود
- **EducationExpert** (کارشناس آموزش) - پشتیبانی و نظارت بر فرآیند آموزش
- **CourseSupport** (پشتیبان دوره) - پشتیبانی فنی و آموزشی دوره‌ها

### 💰 نقش‌های مالی

- **Accountant** (حسابدار) - مدیریت حسابداری و گزارش‌های مالی

### 📈 نقش‌های بازاریابی

- **Marketer** (بازاریاب) - اجرای کمپین‌های بازاریابی

### 👥 نقش‌های کاربری

- **User** (کاربر عادی) - دسترسی محدود به امکانات عمومی
- **Student** (دانشجو) - دسترسی به دوره‌ها و محتوای آموزشی

## 🎯 ماتریس دسترسی‌ها

### 📚 مدیریت دوره‌ها

**دسترسی کامل:**

- Admin, Manager, EducationManager, GeneralManager, DepartmentManager

**دوره‌های شخصی:**

- Instructor, EducationExpert, CourseSupport

### 👥 مدیریت کاربران

**دسترسی کامل:**

- Manager, GeneralManager, ITManager, DepartmentManager

### 🏷️ مدیریت دسته‌بندی‌ها

**دسترسی کامل:**

- Admin, Manager, EducationManager, GeneralManager

### 💰 بخش حسابداری و مالی

**دسترسی کامل:**

- Admin, Manager, GeneralManager, FinancialManager, Accountant

### ⏰ مدیریت زمان‌بندی دوره‌ها

**دسترسی کامل:**

- Admin, Manager, Instructor, EducationManager, EducationExpert, CourseSupport

### 🎛️ پنل مدیریت

**دسترسی به پنل:**

- تمام نقش‌های مدیریتی و آموزشی (به جز User و Student)

## 🔧 پیاده‌سازی فنی

### 1. تعریف نقش‌ها در `src/services/Libs.js`

```javascript
export const ROLE_TRANSLATIONS = {
  Manager: "مدیر سیستم",
  Admin: "ادمین",
  User: "کاربر عادی",
  FinancialManager: "مدیر مالی",
  Instructor: "مدرس",
  Student: "دانشجو",
  ITManager: "مدیر IT",
  MarketingManager: "مدیر مارکتینگ",
  EducationManager: "مدیر آموزش",
  Accountant: "حسابدار",
  GeneralManager: "مدیر کل",
  DepartmentManager: "مدیر دپارتمان",
  CourseSupport: "پشتیبان دوره",
  Marketer: "بازاریاب",
  InternalManager: "مدیر داخلی",
  EducationExpert: "کارشناس آموزش",
};
```

### 2. کنترل دسترسی در مسیرها (`src/App.jsx`)

```jsx
<Route
  path="courses"
  element={
    <RequireRole
      allowedRoles={[
        "Admin",
        "Manager",
        "EducationManager",
        "GeneralManager",
        "DepartmentManager",
      ]}
    >
      <AdminCourses />
    </RequireRole>
  }
/>
```

### 3. کنترل دسترسی در منوی کناری (`src/layouts/AdminLayout.jsx`)

```jsx
{
  hasRole([
    "Admin",
    "Manager",
    "EducationManager",
    "GeneralManager",
    "DepartmentManager",
  ]) && (
    <SidebarItem icon={BookOpen} label="مدیریت دوره‌ها" to="/admin/courses" />
  );
}
```

### 4. کنترل دسترسی در نوار ناوبری (`src/components/Navbar.jsx`)

```jsx
{user && user.roles?.some(role =>
  ['Admin', 'Manager', 'Instructor', 'EducationManager', ...].includes(role)
) && (
  <Link to="/admin">پنل مدیریت</Link>
)}
```

## 🛡️ امنیت و بهترین شیوه‌ها

### 1. اعتبارسنجی چندلایه

- بررسی در سمت کلاینت (UI/UX)
- بررسی در مسیرها (Route Guards)
- بررسی در سمت سرور (API Endpoints)

### 2. اصل کمترین دسترسی (Principle of Least Privilege)

- هر نقش فقط به منابع مورد نیاز دسترسی دارد
- دسترسی‌ها بر اساس مسئولیت‌های شغلی تعریف شده

### 3. مدیریت نقش‌های ترکیبی

- کاربران می‌توانند چندین نقش داشته باشند
- سیستم بر اساس بالاترین سطح دسترسی عمل می‌کند

### 4. حساسیت به حروف بزرگ و کوچک

- تمام بررسی‌های نقش بدون حساسیت به حروف انجام می‌شود
- نرمال‌سازی نقش‌ها در `AuthContext`

## 📊 گزارش‌گیری و نظارت

### 1. لاگ دسترسی‌ها

- ثبت تمام تلاش‌های دسترسی
- نظارت بر فعالیت‌های مشکوک

### 2. مدیریت جلسات

- کنترل زمان انقضای جلسات
- مدیریت توکن‌های دسترسی

### 3. گزارش‌های امنیتی

- گزارش دسترسی‌های غیرمجاز
- تحلیل الگوهای استفاده

## 🔄 به‌روزرسانی و نگهداری

### 1. اضافه کردن نقش جدید

1. اضافه کردن به `ROLE_TRANSLATIONS`
2. به‌روزرسانی `RequireAdmin` layout
3. تنظیم دسترسی‌ها در مسیرها
4. به‌روزرسانی منوی کناری

### 2. تغییر سطح دسترسی

1. بررسی تأثیر بر سایر بخش‌ها
2. به‌روزرسانی تست‌های مربوطه
3. اطلاع‌رسانی به تیم

### 3. حذف نقش

1. بررسی وابستگی‌ها
2. مهاجرت کاربران موجود
3. پاک‌سازی کد مربوطه

## 🎯 مزایای سیستم فعلی

✅ **انعطاف‌پذیری**: امکان تعریف نقش‌های جدید
✅ **امنیت**: کنترل دسترسی چندلایه
✅ **قابلیت نگهداری**: کد تمیز و سازمان‌یافته
✅ **عملکرد**: بررسی‌های بهینه شده
✅ **تجربه کاربری**: نمایش مناسب بر اساس نقش
✅ **مقیاس‌پذیری**: قابلیت گسترش برای سازمان‌های بزرگ

## 📝 نکات مهم برای توسعه‌دهندگان

1. **همیشه از `hasRole()` استفاده کنید** به جای بررسی مستقیم آرایه نقش‌ها
2. **نقش‌ها را در `ROLE_TRANSLATIONS` تعریف کنید** برای ترجمه یکپارچه
3. **تست‌های مربوط به دسترسی را فراموش نکنید**
4. **مستندات را به‌روز نگه دارید** هنگام تغییر نقش‌ها
5. **از اصل کمترین دسترسی پیروی کنید**

---

**آخرین به‌روزرسانی:** دسامبر 2024
**نسخه:** 2.0
**وضعیت:** فعال و کامل ✅
