# 📚 مستندات کامل پیاده‌سازی سیستم زمان‌بندی دوره‌ها

## 🎯 خلاصه پروژه

این پروژه یک سیستم مدیریت دوره‌های آموزشی با قابلیت زمان‌بندی پیشرفته است که شامل پنل ادمین، سیستم ثبت‌نام، و مدیریت کاربران می‌باشد.

## 🚀 تکنولوژی‌های استفاده شده

- **Frontend**: React 18 + Vite
- **Styling**: Tailwind CSS
- **Icons**: Lucide React
- **Routing**: React Router DOM
- **State Management**: React Context API
- **HTTP Client**: Axios
- **Notifications**: React Hot Toast
- **SEO**: React Helmet Async

## 📁 ساختار پروژه

```
src/
├── components/           # کامپوننت‌های قابل استفاده مجدد
│   ├── CourseCard.jsx   # کارت نمایش دوره
│   ├── Editor.jsx       # ویرایشگر متن
│   ├── Navbar.jsx       # نوار ناوبری
│   ├── ScheduleSelector.jsx  # انتخابگر زمان‌بندی ⭐ جدید
│   └── UI.jsx           # کامپوننت‌های UI پایه
├── context/             # مدیریت state سراسری
│   ├── AuthContext.jsx  # احراز هویت
│   └── ThemeContext.jsx # تم تاریک/روشن
├── layouts/             # قالب‌های صفحه
│   └── RequireRole.jsx  # کنترل دسترسی نقش‌ها ⭐ جدید
├── pages/               # صفحات اصلی
│   ├── admin/           # پنل مدیریت
│   │   ├── AdminCategories.jsx
│   │   ├── AdminCourses.jsx
│   │   ├── AdminUsers.jsx
│   │   ├── CourseSchedules.jsx  ⭐ جدید
│   │   └── DashboardHome.jsx
│   ├── auth/            # احراز هویت
│   ├── CategoryPage.jsx
│   ├── Chekout.jsx      # صفحه پرداخت ⭐ بروزرسانی شده
│   ├── CourseDetail.jsx
│   ├── Home.jsx
│   └── UserProfile.jsx  # پروفایل کاربر ⭐ بروزرسانی شده
├── services/            # سرویس‌ها و API
│   ├── api.js           # کلاینت HTTP
│   ├── Directories.js   # مدیریت فایل‌ها
│   └── Libs.js          # توابع کمکی ⭐ بروزرسانی شده
└── App.jsx              # کامپوننت اصلی ⭐ بروزرسانی شده
```

## 🔧 تغییرات و بهبودهای انجام شده

### 1. ✅ رفع خطای Base-64 و مسیریابی لاگین

**مشکل**: خطای `System.FormatException: Base-64 encoding error` و مسیریابی نادرست دکمه لاگین

**راه‌حل**:

- تصحیح مسیریابی از `/courses/` به `/course/` در `CourseDetail.jsx` و `UserProfile.jsx`
- رفع مشکل رمزگذاری Base-64 در احراز هویت

**فایل‌های تغییر یافته**:

- `src/pages/CourseDetail.jsx`
- `src/pages/UserProfile.jsx`

### 2. ✅ رفع خطاهای 403 Forbidden برای کامپوننت‌های ادمین

**مشکل**: خطای 403 زمانی که نقش کاربر Admin/Manager نبود

**راه‌حل**:

- پیاده‌سازی `RequireRole` layout برای کنترل دسترسی
- اضافه کردن بررسی نقش در کامپوننت‌های ادمین
- تصحیح دسترسی نقش `Instructor` به مدیریت دوره‌ها (بدون دسترسی به دسته‌بندی‌ها)

**فایل‌های جدید**:

- `src/layouts/RequireRole.jsx`

**فایل‌های تغییر یافته**:

- `src/pages/admin/AdminCourses.jsx`
- `src/pages/admin/AdminCategories.jsx`
- `src/pages/admin/AdminUsers.jsx`
- `src/pages/admin/DashboardHome.jsx`
- `src/context/AuthContext.jsx`

### 3. ✅ متمرکزسازی توابع تکراری

**مشکل**: توابع تکراری در فایل‌های مختلف

**راه‌حل**:

- انتقال تمام توابع کمکی به `src/services/Libs.js`
- حذف کدهای تکراری از کامپوننت‌ها
- ایجاد توابع استاندارد برای فرمت‌بندی

**توابع منتقل شده**:

```javascript
// توابع اصلی
export const getImageUrl = (path) => { ... }
export const formatPrice = (price) => { ... }
export const formatDate = (date) => { ... }

// ترجمه نقش‌ها
export const ROLE_TRANSLATIONS = { ... }
export const translateRole = (role) => { ... }
export const translateRoles = (roles) => { ... }

// وضعیت‌ها
export const STATUS_LABELS = { ... }
export const STATUS_COLORS = { ... }
export const translateStatus = (status) => { ... }
export const getStatusColor = (status) => { ... }

// زمان‌بندی دوره‌ها ⭐ جدید
export const DAY_NAMES = { ... }
export const COURSE_TYPES = { ... }
export const SCHEDULE_STATUS = { ... }
export const getDayName = (dayOfWeek) => { ... }
export const formatTimeRange = (startTime, endTime) => { ... }
export const formatFullSchedule = (dayOfWeek, startTime, endTime) => { ... }
```

**فایل‌های تغییر یافته**:

- `src/services/Libs.js` ⭐ بروزرسانی عمده
- `src/pages/UserProfile.jsx`
- `src/pages/CourseDetail.jsx`
- `src/pages/Chekout.jsx`
- `src/pages/admin/AdminCourses.jsx`
- `src/pages/admin/DashboardHome.jsx`
- `src/layouts/AdminLayout.jsx`

### 4. ⭐ پیاده‌سازی سیستم زمان‌بندی دوره‌ها

**ویژگی‌های جدید**:

#### 4.1. کامپوننت انتخاب زمان‌بندی (`ScheduleSelector.jsx`)

```javascript
// ویژگی‌ها:
- نمایش لیست زمان‌بندی‌های موجود
- نمایش ظرفیت و تعداد ثبت‌نام‌شدگان
- نوار پیشرفت ظرفیت با رنگ‌بندی
- غیرفعال کردن زمان‌بندی‌های پر
- انتخاب زمان‌بندی برای ثبت‌نام
```

#### 4.2. صفحه مدیریت زمان‌بندی (`CourseSchedules.jsx`)

```javascript
// ویژگی‌ها:
- مشاهده لیست زمان‌بندی‌های هر دوره
- ایجاد زمان‌بندی جدید با مودال
- نمایش اطلاعات کامل هر زمان‌بندی
- مشاهده لیست دانشجویان هر زمان‌بندی
- نوار پیشرفت ظرفیت
- وضعیت فعال/غیرفعال
```

#### 4.3. بروزرسانی صفحه پرداخت (`Checkout.jsx`)

```javascript
// بهبودهای جدید:
- فرآیند 4 مرحله‌ای: زمان‌بندی → بازبینی → پرداخت → موفقیت
- انتخاب زمان‌بندی در مرحله اول
- رد کردن مرحله زمان‌بندی اگر دوره زمان‌بندی نداشت
- نمایش زمان‌بندی انتخاب شده در مرحله بازبینی
- ثبت‌نام در زمان‌بندی خاص یا عادی
```

#### 4.4. بروزرسانی مسیریابی (`App.jsx`)

```javascript
// مسیر جدید:
<Route
  path="/admin/courses/:courseId/schedules"
  element={<CourseSchedules />}
/>
```

#### 4.5. بروزرسانی لیست دوره‌های ادمین (`AdminCourses.jsx`)

```javascript
// دکمه جدید:
<Button onClick={() => navigate(`/admin/courses/${course.id}/schedules`)}>
  مدیریت زمان‌بندی
</Button>
```

### 5. ✅ رفع خطاهای lucide-react

**مشکل**: خطای `Cannot read properties of undefined (reading 'map')` در آیکون‌ها

**راه‌حل**:

- جایگزینی آیکون‌های مشکل‌دار با SVG های inline در `CourseSchedules.jsx`
- رفع مشکل `icon` prop در `UserProfile.jsx`

**فایل‌های تغییر یافته**:

- `src/pages/admin/CourseSchedules.jsx`
- `src/pages/UserProfile.jsx`

## 🔴 مشکل اصلی: Backend API

### مشکل شناسایی شده

**مشکل**: API endpoint `/courses` آرایه `schedules` را همیشه خالی برمی‌گرداند:

```json
{
  "id": "7f4259d1-ea16-4541-e413-08de34f16021",
  "title": "saaa",
  "schedules": [] // ⚠️ همیشه خالی است
}
```

### تأثیر مشکل

1. **صفحه مدیریت زمان‌بندی**: زمان‌بندی‌های ایجاد شده بعد از رفرش نمایش داده نمی‌شوند
2. **صفحه پرداخت**: زمان‌بندی‌ها در checkout نمایش داده نمی‌شوند
3. **تجربه کاربری**: کاربران نمی‌توانند زمان‌بندی انتخاب کنند

### راه‌حل موقت پیاده‌سازی شده

```javascript
// در CourseSchedules.jsx
// schedules همیشه خالی است در /courses endpoint
// پس فقط آرایه خالی تنظیم می‌کنیم و schedules بعداً از state مدیریت می‌شوند
setSchedules([]);

// در Checkout.jsx
// اگر schedules خالی باشد، مرحله انتخاب زمان‌بندی را رد کن
if (!foundCourse.schedules || foundCourse.schedules.length === 0) {
  foundCourse.schedules = [];
}
```

### راه‌حل نهایی (نیاز به تیم Backend)

**گزینه 1**: تصحیح endpoint `/courses` برای include کردن schedules

```csharp
// در Backend Controller
return courses.Include(c => c.Schedules).ToList();
```

**گزینه 2**: ایجاد endpoint جداگانه برای schedules

```
GET /courses/{courseId}/schedules
```

## 📋 API Endpoints مورد استفاده

### موجود و کارکرد

- `GET /courses` - دریافت لیست دوره‌ها ⚠️ (schedules خالی)
- `POST /courses/{courseId}/schedules` - ایجاد زمان‌بندی ✅
- `GET /courses/{courseId}/schedules/{scheduleId}/students` - دانشجویان زمان‌بندی ✅
- `POST /courses/{courseId}/schedules/{scheduleId}/enroll` - ثبت‌نام در زمان‌بندی ✅
- `POST /courses/{courseId}/enroll` - ثبت‌نام عادی ✅

### نیاز به تصحیح

- `GET /courses` - باید schedules را include کند
- `GET /courses/{courseId}/schedules` - برای دریافت schedules (اختیاری)

## 🎨 طراحی UI/UX

### رنگ‌بندی و تم

- **Primary**: Indigo (#4F46E5)
- **Success**: Emerald (#10B981)
- **Warning**: Amber (#F59E0B)
- **Error**: Red (#EF4444)
- **تم تاریک**: پشتیبانی کامل

### کامپوننت‌های طراحی شده

- **مودال‌های مدرن**: گوشه‌های گرد، سایه‌های نرم
- **نوار پیشرفت**: رنگ‌بندی بر اساس ظرفیت
- **کارت‌های تعاملی**: Hover effects و انیمیشن‌ها
- **فرم‌های کاربرپسند**: Validation و پیام‌های خطا

## 🔧 نصب و راه‌اندازی

### پیش‌نیازها

```bash
Node.js >= 18
npm >= 8
```

### نصب

```bash
# کلون پروژه
git clone [repository-url]

# نصب dependencies
npm install

# اجرای پروژه
npm run dev
```

### متغیرهای محیطی

```env
VITE_API_BASE_URL=https://localhost:44367
```

## 📝 نحوه استفاده

### برای ادمین‌ها

1. ورود به پنل ادمین
2. رفتن به بخش "مدیریت دوره‌ها"
3. انتخاب دوره و کلیک روی "مدیریت زمان‌بندی"
4. ایجاد زمان‌بندی‌های جدید
5. مشاهده و مدیریت دانشجویان

### برای کاربران

1. انتخاب دوره مورد نظر
2. کلیک روی "ثبت‌نام در دوره"
3. انتخاب زمان‌بندی (در صورت وجود)
4. تکمیل فرآیند پرداخت
5. دسترسی به دوره در پنل کاربری

## 🐛 مشکلات شناخته شده

### 1. مشکل اصلی Backend

- **وضعیت**: 🔴 بحرانی
- **توضیح**: API `/courses` schedules را برنمی‌گرداند
- **تأثیر**: عدم نمایش زمان‌بندی‌ها
- **راه‌حل**: نیاز به تصحیح Backend

### 2. مشکلات فرعی

- **Performance**: بارگذاری تصاویر ممکن است کند باشد
- **Caching**: نیاز به cache کردن داده‌های دوره‌ها
- **Offline**: عدم پشتیبانی از حالت آفلاین

## 🚀 بهبودهای آینده

### فاز 1 (اولویت بالا)

- [ ] رفع مشکل Backend برای schedules
- [ ] پیاده‌سازی ویرایش و حذف زمان‌بندی
- [ ] اضافه کردن فیلتر و جستجو در لیست زمان‌بندی‌ها

### فاز 2 (اولویت متوسط)

- [ ] پیاده‌سازی تقویم برای نمایش زمان‌بندی‌ها
- [ ] سیستم اعلان‌ها برای تغییرات زمان‌بندی
- [ ] گزارش‌گیری از حضور و غیاب

### فاز 3 (اولویت پایین)

- [ ] پیاده‌سازی PWA
- [ ] اضافه کردن Dark Mode toggle
- [ ] بهینه‌سازی برای موبایل

## 👥 تیم توسعه

### Frontend Developer

- پیاده‌سازی کامپوننت‌های React
- طراحی UI/UX
- مدیریت State
- تست و دیباگ

### Backend Developer (مورد نیاز)

- تصحیح API endpoints
- پیاده‌سازی Include برای schedules
- بهینه‌سازی کوئری‌ها
- مدیریت دیتابیس

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال:

1. بررسی مستندات
2. چک کردن Console برای خطاها
3. بررسی Network tab برای API calls
4. تماس با تیم Backend برای مشکل schedules

---

## 📊 خلاصه وضعیت پروژه

| بخش                        | وضعیت   | درصد تکمیل |
| -------------------------- | ------- | ---------- |
| احراز هویت                 | ✅ کامل | 100%       |
| مدیریت کاربران             | ✅ کامل | 100%       |
| مدیریت دوره‌ها             | ✅ کامل | 100%       |
| سیستم زمان‌بندی (Frontend) | ✅ کامل | 100%       |
| سیستم زمان‌بندی (Backend)  | ⚠️ مشکل | 60%        |
| صفحه پرداخت                | ✅ کامل | 100%       |
| پنل کاربری                 | ✅ کامل | 100%       |
| UI/UX                      | ✅ کامل | 100%       |

**وضعیت کلی پروژه**: 🟡 آماده با یک مشکل Backend بحرانی

**اولویت فوری**: رفع مشکل API `/courses` برای نمایش schedules
