# 📚 مستندات پیاده‌سازی فرانت‌اند سیستم زمان‌بندی دوره‌ها

## 📋 خلاصه پروژه

این مستند شامل تمام تغییرات و اضافات انجام شده برای پیاده‌سازی سیستم زمان‌بندی دوره‌ها در فرانت‌اند React است. سیستم امکان انتخاب زمان‌های مختلف برای هر دوره و مدیریت زمان‌بندی‌ها را فراهم می‌کند.

---

## 🎯 هدف سیستم

- **برای دانشجویان:** امکان انتخاب زمان‌بندی مناسب هنگام ثبت‌نام در دوره
- **برای مدرسین/ادمین:** مدیریت زمان‌بندی‌های مختلف هر دوره و مشاهده لیست دانشجویان

**مثال:** دوره "آموزش React" می‌تواند در زمان‌های مختلف برگزار شود:

- شنبه‌ها ساعت 10:00-12:00 (گروه صبح) - ظرفیت 20 نفر
- چهارشنبه‌ها ساعت 18:00-20:00 (گروه عصر) - ظرفیت 15 نفر

---

## 🆕 فایل‌های جدید ایجاد شده

### 1. `src/components/ScheduleSelector.jsx`

**کامپوننت انتخاب زمان‌بندی برای دانشجویان**

```jsx
// ویژگی‌های کلیدی:
- نمایش لیست زمان‌بندی‌های موجود
- نمایش ظرفیت و تعداد ثبت‌نام شده
- نوار پیشرفت ظرفیت با رنگ‌بندی (سبز/زرد/قرمز)
- حالت انتخاب شده با تیک سبز
- غیرفعال کردن زمان‌بندی‌های پر
- پشتیبانی از حالت تاریک
- انیمیشن‌های نرم
```

**Props:**

- `schedules`: آرایه زمان‌بندی‌ها
- `onScheduleSelect`: تابع انتخاب زمان‌بندی
- `selectedScheduleId`: ID زمان‌بندی انتخاب شده
- `loading`: حالت لودینگ

### 2. `src/pages/admin/CourseSchedules.jsx`

**صفحه مدیریت زمان‌بندی‌های دوره برای ادمین/مدرس**

```jsx
// ویژگی‌های کلیدی:
- لیست تمام زمان‌بندی‌های یک دوره
- فرم ایجاد زمان‌بندی جدید (مودال)
- نمایش اطلاعات کامل هر زمان‌بندی
- مشاهده لیست دانشجویان هر زمان‌بندی
- نوار پیشرفت ظرفیت
- دکمه‌های ویرایش و حذف
- اعتبارسنجی فرم‌ها
```

**Route:** `/admin/courses/:courseId/schedules`

**دسترسی:** Admin, Manager, Instructor

---

## 🔄 فایل‌های تغییر یافته

### 1. `src/pages/Chekout.jsx`

**تغییرات عمده در فرآیند ثبت‌نام**

#### قبل از تغییر:

```jsx
// 3 مرحله: بازبینی → پرداخت → موفقیت
const [step, setStep] = useState(1);
```

#### بعد از تغییر:

```jsx
// 4 مرحله: زمان‌بندی → بازبینی → پرداخت → موفقیت
const [step, setStep] = useState(1);
const [selectedScheduleId, setSelectedScheduleId] = useState(null);

// مرحله 1: انتخاب زمان‌بندی
{
  step === 1 && (
    <ScheduleSelector
      schedules={course.schedules || []}
      onScheduleSelect={setSelectedScheduleId}
      selectedScheduleId={selectedScheduleId}
      loading={loading}
    />
  );
}
```

#### تغییرات API Call:

```jsx
// قبل:
await api.post(`/courses/${course.id}/enroll`);

// بعد:
if (selectedScheduleId) {
  await api.post(
    `/courses/${course.id}/schedules/${selectedScheduleId}/enroll`
  );
} else {
  await api.post(`/courses/${course.id}/enroll`);
}
```

#### تغییرات Stepper:

```jsx
// اضافه شدن مرحله زمان‌بندی
<div className="flex items-center gap-2 px-4 py-2 rounded-full">
  <span>1</span>
  <span>زمان‌بندی</span>
</div>
```

### 2. `src/pages/admin/AdminCourses.jsx`

**اضافه شدن دکمه مدیریت زمان‌بندی**

#### Import جدید:

```jsx
import { useNavigate } from "react-router-dom";
```

#### دکمه جدید در Actions:

```jsx
// قبل: فقط ویرایش و حذف
<button onClick={() => handleEditClick(course)}>
    <Edit size={18} />
</button>
<button onClick={() => handleDelete(course.id)}>
    <Trash2 size={18} />
</button>

// بعد: اضافه شدن دکمه زمان‌بندی
<button
    onClick={() => navigate(`/admin/courses/${course.id}/schedules`)}
    title="مدیریت زمان‌بندی"
>
    <Clock size={18} />
</button>
```

### 3. `src/App.jsx`

**اضافه شدن Route جدید**

#### Import جدید:

```jsx
import CourseSchedules from "./pages/admin/CourseSchedules";
```

#### Route جدید:

```jsx
<Route
  path="course/:courseId/schedules"
  element={
    <RequireRole allowedRoles={["Admin", "Manager", "Instructor"]}>
      <CourseSchedules />
    </RequireRole>
  }
/>
```

### 4. `src/services/Libs.js`

**اضافه شدن توابع کمکی زمان‌بندی**

#### Constants جدید:

```jsx
export const DAY_NAMES = {
  0: "یکشنبه",
  1: "دوشنبه",
  2: "سه‌شنبه",
  3: "چهارشنبه",
  4: "پنج‌شنبه",
  5: "جمعه",
  6: "شنبه",
};

export const COURSE_TYPES = {
  Online: "آنلاین",
  InPerson: "حضوری",
  Hybrid: "ترکیبی",
};

export const SCHEDULE_STATUS = {
  Active: "فعال",
  Transferred: "انتقال یافته",
  Withdrawn: "انصراف",
  Expelled: "اخراج",
};
```

#### توابع جدید:

```jsx
export const getDayName = (dayOfWeek) => {
  return DAY_NAMES[dayOfWeek] || "نامشخص";
};

export const getCourseTypeName = (type) => {
  return COURSE_TYPES[type] || type;
};

export const getScheduleStatusName = (status) => {
  return SCHEDULE_STATUS[status] || status;
};

export const formatTimeRange = (startTime, endTime) => {
  return `${startTime}-${endTime}`;
};

export const formatFullSchedule = (dayOfWeek, startTime, endTime) => {
  return `${getDayName(dayOfWeek)} ${formatTimeRange(startTime, endTime)}`;
};
```

---

## 🎨 طراحی UI/UX

### رنگ‌بندی و تم

- **رنگ اصلی:** Indigo (برای دکمه‌ها و عناصر فعال)
- **رنگ موفقیت:** Emerald (برای وضعیت‌های مثبت)
- **رنگ هشدار:** Amber (برای ظرفیت نزدیک به اتمام)
- **رنگ خطر:** Red (برای ظرفیت تمام شده)

### حالت تاریک

- پشتیبانی کامل از Dark Mode
- تغییر رنگ پس‌زمینه و متن‌ها
- حفظ کنتراست مناسب

### انیمیشن‌ها

```css
/* انیمیشن ورود */
animate-in fade-in slide-in-from-right-8 duration-500

/* انیمیشن hover */
hover:-translate-y-1 transition-transform

/* انیمیشن لودینگ */
animate-spin w-10 h-10 border-4 border-indigo-600
```

---

## 📱 تجربه کاربری (UX)

### برای دانشجویان:

1. **ورود به صفحه دوره** → مشاهده زمان‌بندی‌های موجود
2. **کلیک روی ثبت‌نام** → انتقال به صفحه Checkout
3. **انتخاب زمان‌بندی** → مشاهده ظرفیت و جزئیات
4. **بازبینی انتخاب** → تأیید زمان‌بندی انتخاب شده
5. **پرداخت** → تکمیل فرآیند ثبت‌نام

### برای مدرسین/ادمین:

1. **ورود به پنل ادمین** → مشاهده لیست دوره‌ها
2. **کلیک روی آیکون ساعت** → ورود به مدیریت زمان‌بندی
3. **ایجاد زمان‌بندی جدید** → تعیین روز، ساعت، ظرفیت
4. **مشاهده دانشجویان** → لیست ثبت‌نام شدگان هر زمان‌بندی

---

## 🔧 جزئیات فنی

### State Management

```jsx
// در Checkout.jsx
const [selectedScheduleId, setSelectedScheduleId] = useState(null);
const [step, setStep] = useState(1); // 4 مرحله

// در CourseSchedules.jsx
const [schedules, setSchedules] = useState([]);
const [showCreateModal, setShowCreateModal] = useState(false);
const [formData, setFormData] = useState({
  title: "",
  dayOfWeek: 0,
  startTime: "",
  endTime: "",
  maxCapacity: 20,
  description: "",
});
```

### API Endpoints مورد استفاده

```javascript
// ثبت‌نام در زمان‌بندی خاص
POST / courses / { courseId } / schedules / { scheduleId } / enroll;

// ایجاد زمان‌بندی جدید
POST / courses / { courseId } / schedules;

// دریافت لیست دانشجویان
GET / courses / { courseId } / schedules / { scheduleId } / students;

// دریافت اطلاعات دوره شامل زمان‌بندی‌ها
GET / courses / { courseId };

// ثبت‌نام عادی در دوره (بدون زمان‌بندی)
POST / courses / { courseId } / enroll;
```

### Validation Rules

```jsx
// اعتبارسنجی فرم زمان‌بندی
- title: الزامی، حداقل 2 کاراکتر
- startTime: الزامی، فرمت HH:MM
- endTime: الزامی، باید بزرگتر از startTime باشد
- maxCapacity: الزامی، عدد بین 1 تا 100
- dayOfWeek: الزامی، عدد بین 0 تا 6
```

---

## 🔒 کنترل دسترسی

### سطوح دسترسی:

```jsx
// مشاهده و ایجاد زمان‌بندی
allowedRoles: ["Admin", "Manager", "Instructor"];

// ثبت‌نام در زمان‌بندی
// همه کاربران لاگین شده (پس از ثبت‌نام در دوره اصلی)

// مشاهده لیست دانشجویان
allowedRoles: ["Admin", "Manager", "Instructor"];
// (فقط دوره‌های مربوط به خودشان برای Instructor)
```

---

## 📊 ساختار داده

### Schedule Object:

```javascript
{
  id: "guid",
  title: "گروه صبح",
  dayOfWeek: 6, // شنبه
  startTime: "10:00",
  endTime: "12:00",
  maxCapacity: 20,
  enrolledCount: 15,
  remainingCapacity: 5,
  hasCapacity: true,
  isActive: true,
  description: "کلاس حضوری",
  fullScheduleText: "شنبه 10:00-12:00"
}
```

### Student Object:

```javascript
{
  userId: "string",
  fullName: "احمد محمدی",
  email: "ahmad@example.com",
  mobile: "09123456789",
  enrolledAt: "2024-12-12T10:00:00Z",
  status: "Active",
  attendedSessions: 5,
  absentSessions: 1,
  instructorNotes: "دانشجوی فعال"
}
```

---

## 🚀 نحوه استفاده

### برای توسعه‌دهندگان:

#### 1. اضافه کردن زمان‌بندی به دوره موجود:

```jsx
// در CourseDetail.jsx - زمان‌بندی‌ها خودکار نمایش داده می‌شوند
{
  course.schedules && course.schedules.length > 0 && (
    <div className="space-y-2">
      {course.schedules.map((schedule) => (
        <div key={schedule.id}>
          {schedule.fullScheduleText}
          <span>
            {schedule.enrolledCount}/{schedule.maxCapacity}
          </span>
        </div>
      ))}
    </div>
  );
}
```

#### 2. استفاده از ScheduleSelector:

```jsx
import ScheduleSelector from "../components/ScheduleSelector";

<ScheduleSelector
  schedules={courseSchedules}
  onScheduleSelect={(scheduleId) => setSelectedSchedule(scheduleId)}
  selectedScheduleId={selectedSchedule}
  loading={false}
/>;
```

#### 3. اضافه کردن توابع کمکی جدید:

```jsx
// در src/services/Libs.js
export const formatScheduleTime = (schedule) => {
  return `${getDayName(schedule.dayOfWeek)} ${formatTimeRange(
    schedule.startTime,
    schedule.endTime
  )}`;
};
```

---

## 🧪 تست و بررسی

### چک‌لیست تست:

#### تست دانشجو:

- [ ] مشاهده زمان‌بندی‌ها در صفحه دوره
- [ ] انتخاب زمان‌بندی در Checkout
- [ ] عدم امکان انتخاب زمان‌بندی پر
- [ ] نمایش صحیح ظرفیت باقی‌مانده
- [ ] ثبت‌نام موفق در زمان‌بندی

#### تست ادمین/مدرس:

- [ ] دسترسی به صفحه مدیریت زمان‌بندی
- [ ] ایجاد زمان‌بندی جدید
- [ ] اعتبارسنجی فرم‌ها
- [ ] مشاهده لیست دانشجویان
- [ ] نمایش صحیح آمار ظرفیت

#### تست عمومی:

- [ ] عملکرد در حالت تاریک
- [ ] ریسپانسیو بودن در موبایل
- [ ] نمایش صحیح پیام‌های خطا
- [ ] انیمیشن‌های نرم

---

## 🔮 قابلیت‌های آینده

### پیشنهادات توسعه:

1. **تقویم زمان‌بندی:** نمایش تقویم ماهانه زمان‌بندی‌ها
2. **اعلان‌ها:** اطلاع‌رسانی تغییرات زمان‌بندی
3. **حضور و غیاب:** سیستم چک‌این برای کلاس‌ها
4. **گزارش‌گیری:** آمار تفصیلی حضور دانشجویان
5. **تداخل زمانی:** بررسی تداخل زمان‌بندی‌های کاربر
6. **ظرفیت انتظار:** لیست انتظار برای زمان‌بندی‌های پر

---

## 📞 پشتیبانی و مستندات

### فایل‌های مرجع:

- `README_CourseSchedule_System.md` - مستندات کامل بک‌اند
- `src/components/ScheduleSelector.jsx` - کامپوننت انتخاب زمان‌بندی
- `src/pages/admin/CourseSchedules.jsx` - صفحه مدیریت زمان‌بندی
- `src/services/Libs.js` - توابع کمکی

### نکات مهم:

- همیشه از توابع `Libs.js` برای فرمت کردن استفاده کنید
- کنترل دسترسی را در نظر بگیرید
- اعتبارسنجی سمت کلاینت الزامی است
- پیام‌های خطا باید کاربرپسند باشند

---

**📝 نکته:** این سیستم کاملاً آماده استفاده است و تمام ویژگی‌های مورد نیاز برای مدیریت زمان‌بندی دوره‌ها را پوشش می‌دهد.

---

## 🚨 مشکل بحرانی شناسایی شده

### مشکل Backend API

**وضعیت:** 🔴 بحرانی - نیاز به رفع فوری

**مشکل:** API endpoint `/courses` آرایه `schedules` را همیشه خالی برمی‌گرداند:

```json
{
  "id": "7f4259d1-ea16-4541-e413-08de34f16021",
  "title": "saaa",
  "schedules": [] // ⚠️ همیشه خالی است حتی اگر زمان‌بندی ایجاد شده باشد
}
```

### تأثیر مشکل:

1. **صفحه مدیریت زمان‌بندی**: زمان‌بندی‌های ایجاد شده بعد از رفرش صفحه نمایش داده نمی‌شوند
2. **صفحه پرداخت**: زمان‌بندی‌ها در checkout اصلاً نمایش داده نمی‌شوند
3. **تجربه کاربری**: کاربران نمی‌توانند زمان‌بندی انتخاب کنند

### راه‌حل موقت (پیاده‌سازی شده):

```javascript
// در Checkout.jsx - رد کردن مرحله زمان‌بندی اگر خالی باشد
if (!foundCourse.schedules || foundCourse.schedules.length === 0) {
  foundCourse.schedules = [];
  // مستقیماً به مرحله 2 (بازبینی) می‌رود
}

// در CourseSchedules.jsx - مدیریت schedules از طریق state محلی
setSchedules([]); // همیشه خالی از API
// schedules جدید فقط در state محلی نگهداری می‌شوند
```

### راه‌حل نهایی (نیاز به تیم Backend):

**گزینه 1 (توصیه شده):** تصحیح endpoint `/courses`

```csharp
// در Backend Controller
return courses.Include(c => c.Schedules).ToList();
```

**گزینه 2:** ایجاد endpoint جداگانه

```
GET /courses/{courseId}/schedules
```

### وضعیت فعلی:

- ✅ **Frontend**: کاملاً پیاده‌سازی شده و آماده
- ❌ **Backend**: نیاز به تصحیح API برای include کردن schedules
- ⚠️ **عملکرد**: سیستم کار می‌کند اما schedules نمایش داده نمی‌شوند

**اولویت فوری:** رفع مشکل Backend برای نمایش schedules

---

**🔄 آخرین بروزرسانی:** 13 دسامبر 2024

**👨‍💻 توسعه‌دهنده:** Kiro AI Assistant

**📊 وضعیت پروژه:** 🟡 آماده با یک مشکل Backend بحرانی
