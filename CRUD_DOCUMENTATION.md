# 🎯 مستندات CRUD اسلایدها و استوری‌ها

## 📋 فهرست مطالب

- [معرفی](#معرفی)
- [ساختار فایل‌ها](#ساختار-فایلها)
- [API Endpoints](#api-endpoints)
- [صفحات مدیریت](#صفحات-مدیریت)
- [ویژگی‌ها](#ویژگیها)
- [نحوه استفاده](#نحوه-استفاده)

## 🎨 معرفی

این سیستم CRUD کامل برای مدیریت اسلایدهای اصلی و استوری‌های موفقیت طراحی شده است. شامل:

- ✅ **مدیریت اسلایدهای اصلی (Hero Slides)**
- ✅ **مدیریت استوری‌های موفقیت (Success Stories)**
- ✅ **داشبورد آماری کامل**
- ✅ **رابط کاربری حرفه‌ای و responsive**
- ✅ **پشتیبانی کامل از API backend**

## 📁 ساختار فایل‌ها

```
src/
├── pages/admin/
│   ├── SlidesManagement.jsx      # مدیریت اسلایدهای اصلی
│   ├── StoriesManagement.jsx     # مدیریت استوری‌های موفقیت
│   └── ContentDashboard.jsx      # داشبورد مدیریت محتوا
├── components/admin/
│   ├── ContentStats.jsx          # کامپوننت آمار محتوا
│   └── SliderManager.jsx         # مدیریت ترکیبی (قدیمی)
└── App.jsx                       # تعریف route ها
```

## 🔗 API Endpoints

### اسلایدهای اصلی (Hero Slides)

- `GET /api/HeroSlides` - دریافت لیست اسلایدها
- `GET /api/HeroSlides/active` - دریافت اسلایدهای فعال
- `GET /api/HeroSlides/{id}` - دریافت یک اسلاید
- `POST /api/HeroSlides` - ایجاد اسلاید جدید
- `PUT /api/HeroSlides/{id}` - به‌روزرسانی اسلاید
- `DELETE /api/HeroSlides/{id}` - حذف اسلاید

### استوری‌های موفقیت (Success Stories)

- `GET /api/SuccessStories` - دریافت لیست استوری‌ها
- `GET /api/SuccessStories/active` - دریافت استوری‌های فعال
- `GET /api/SuccessStories/{id}` - دریافت یک استوری
- `POST /api/SuccessStories` - ایجاد استوری جدید
- `PUT /api/SuccessStories/{id}` - به‌روزرسانی استوری
- `DELETE /api/SuccessStories/{id}` - حذف استوری

## 🖥️ صفحات مدیریت

### 1. داشبورد مدیریت محتوا (`/admin/content`)

- **آمار کلی**: تعداد اسلایدها، استوری‌ها، وضعیت فعال/غیرفعال
- **عملیات سریع**: دسترسی سریع به ایجاد محتوای جدید
- **آخرین آیتم‌ها**: نمایش آخرین اسلایدها و استوری‌های اضافه شده
- **هشدارهای انقضا**: نمایش محتوای منقضی شده یا نزدیک به انقضا

### 2. مدیریت اسلایدها (`/admin/slides`)

- **لیست اسلایدها**: نمایش grid کامل اسلایدها
- **فیلترها**: فیلتر بر اساس وضعیت (فعال/غیرفعال)
- **جستجو**: جستجو در عنوان و توضیحات
- **عملیات CRUD**: ایجاد، ویرایش، حذف، تغییر وضعیت

### 3. مدیریت استوری‌ها (`/admin/stories`)

- **لیست استوری‌ها**: نمایش grid کامل استوری‌ها
- **فیلترهای پیشرفته**: فیلتر بر اساس وضعیت و نوع
- **انواع استوری**: موفقیت، ویدیو، نظرات
- **عملیات CRUD**: ایجاد، ویرایش، حذف، تغییر وضعیت

## ✨ ویژگی‌ها

### ویژگی‌های مشترک

- ✅ **Responsive Design**: سازگار با تمام دستگاه‌ها
- ✅ **Dark Mode**: پشتیبانی کامل از حالت تاریک
- ✅ **Real-time Updates**: به‌روزرسانی فوری پس از تغییرات
- ✅ **Error Handling**: مدیریت خطاها با toast notifications
- ✅ **Loading States**: نمایش وضعیت بارگذاری
- ✅ **Form Validation**: اعتبارسنجی فرم‌ها

### ویژگی‌های اسلایدها

- 🖼️ **مدیریت تصویر**: آپلود فایل یا URL
- 🏷️ **برچسب‌ها**: اضافه کردن برچسب‌های سفارشی
- 🔗 **دکمه‌های عمل**: دکمه اصلی و ثانویه
- 📊 **آمار**: اضافه کردن آمار به اسلاید
- ⏰ **انقضا**: اسلایدهای دائمی یا موقت
- 📋 **ترتیب**: تعیین ترتیب نمایش

### ویژگی‌های استوری‌ها

- 👤 **اطلاعات دانشجو**: نام دانشجو و دوره
- 🎭 **انواع مختلف**: موفقیت، ویدیو، نظرات
- ⏱️ **مدت زمان**: تنظیم مدت زمان نمایش
- 🔗 **لینک عمل**: دکمه عمل با لینک
- 📈 **آمار**: اضافه کردن آمار به استوری
- 🎯 **دوره مرتبط**: ارتباط با دوره‌های موجود

## 🚀 نحوه استفاده

### دسترسی به صفحات

```
/admin/content  - داشبورد مدیریت محتوا
/admin/slides   - مدیریت اسلایدها
/admin/stories  - مدیریت استوری‌ها
/admin/sliders  - مدیریت ترکیبی (قدیمی)
```

### مجوزهای دسترسی

فقط کاربران با نقش‌های زیر دسترسی دارند:

- `Admin`
- `Manager`
- `GeneralManager`
- `EducationManager`
- `ContentManager`

### مراحل ایجاد اسلاید جدید

1. رفتن به `/admin/slides`
2. کلیک روی "اسلاید جدید"
3. پر کردن فرم:
   - عنوان (اجباری)
   - توضیحات
   - تصویر (فایل یا URL)
   - برچسب
   - دکمه‌های عمل
   - آمار (اختیاری)
   - نوع (دائمی/موقت)
4. ذخیره

### مراحل ایجاد استوری جدید

1. رفتن به `/admin/stories`
2. کلیک روی "استوری جدید"
3. پر کردن فرم:
   - عنوان (اجباری)
   - زیرعنوان
   - توضیحات
   - نوع استوری
   - تصویر
   - اطلاعات دانشجو
   - دکمه عمل
   - آمار (اختیاری)
   - نوع (دائمی/موقت)
4. ذخیره

## 🔧 تنظیمات پیشرفته

### فیلدهای اجباری

**اسلایدها:**

- Title (عنوان)

**استوری‌ها:**

- Title (عنوان)

### فیلدهای اختیاری

**اسلایدها:**

- Description, ImageUrl/ImageFile, Badge, PrimaryActionLabel, PrimaryActionLink, SecondaryActionLabel, SecondaryActionLink, Stats, Order, IsPermanent, ExpiresAt

**استوری‌ها:**

- Subtitle, Description, ImageUrl/ImageFile, Badge, Type, StudentName, CourseName, ActionLabel, ActionLink, Stats, Duration, CourseId, Order, IsPermanent, ExpiresAt

## 📊 آمار و گزارش‌ها

### آمار موجود در داشبورد

- تعداد کل اسلایدها و استوری‌ها
- تعداد محتوای فعال و غیرفعال
- تعداد محتوای دائمی و موقت
- تعداد محتوای منقضی شده
- نرخ فعال بودن محتوا
- آخرین محتوای اضافه شده
- محتوای نزدیک به انقضا

### نمودارها

- نمودار میله‌ای وضعیت محتوا
- نمودار دایره‌ای انواع استوری‌ها
- نمودار خطی روند اضافه شدن محتوا

## 🛠️ توسعه و سفارشی‌سازی

### اضافه کردن فیلد جدید

1. اضافه کردن فیلد به `initialFormData`
2. اضافه کردن input در modal
3. اضافه کردن فیلد به `FormData` در توابع create/update
4. به‌روزرسانی API backend

### اضافه کردن فیلتر جدید

1. اضافه کردن state جدید
2. اضافه کردن دکمه فیلتر در UI
3. اضافه کردن منطق فیلتر در `filteredItems`

### سفارشی‌سازی UI

- تغییر رنگ‌ها در `tailwind.config.js`
- تغییر آیکون‌ها از `lucide-react`
- اضافه کردن انیمیشن‌های جدید

## 🐛 عیب‌یابی

### مشکلات رایج

1. **خطای 401**: بررسی token احراز هویت
2. **خطای 403**: بررسی مجوزهای کاربر
3. **خطای 404**: بررسی صحت API endpoints
4. **خطای آپلود تصویر**: بررسی سایز و فرمت فایل

### لاگ‌ها

- خطاهای API در browser console
- خطاهای network در Network tab
- خطاهای validation در toast notifications

## 📝 نکات مهم

1. **امنیت**: همه عملیات نیاز به احراز هویت دارند
2. **Performance**: تصاویر بهینه‌سازی شوند
3. **UX**: پیام‌های مناسب برای کاربر نمایش داده شود
4. **Validation**: اعتبارسنجی در frontend و backend
5. **Backup**: قبل از حذف، تأیید از کاربر گرفته شود

## 🔄 به‌روزرسانی‌های آینده

- [ ] ویرایش گروهی
- [ ] خروجی Excel
- [ ] ورودی گروهی از Excel
- [ ] پیش‌نمایش زنده
- [ ] تنظیمات پیشرفته
- [ ] سیستم نظرات
- [ ] تاریخچه تغییرات
- [ ] بازیافت محتوای حذف شده

---

**نسخه:** 1.0.0  
**تاریخ:** دسامبر 2024  
**توسعه‌دهنده:** تیم فنی پردیس
